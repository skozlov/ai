package com.github.skozlov.ai.brain

import com.github.skozlov.ai.brain.Brain.{Outputs, Inputs}

private[brain] class Memory {
	def onPerception(inputs: Inputs): Unit ={
		???
	}

	def onAction(outputs: Outputs): Unit ={
		???
	}
}
